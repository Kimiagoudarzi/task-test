import { HomeProps } from "@types";

export default async function Home({ searchParams }: HomeProps) {

  return (
    <main>
      this is the main page
      <h1>دعا کن فونت درست شده باشه</h1>
    </main>
  );
}
